# 复制本文件为 .streamlit/secrets.toml 并填入真实值(secrets.toml 已 gitignore,不入库)。
# 云端在 Streamlit Cloud → App → Settings → Secrets 填同样内容。

# DeepSeek API Key(必填):AI 展开因果路径用。也可在页面密码框临时输入(仅当次会话)。
deepseek_api_key = "sk-..."

# 访问口令(选填):配置后访客需输入此口令才能进入;留空/不配则不拦(本地开发方便)。
app_password = "请设置一个口令"
