# 运行时依赖(版本已锁定)。建议 Python 3.11。
# 交付版界面为 NiceGUI(app_nicegui.py);旧的 Streamlit 版(app.py)亦保留,如需运行可解开 streamlit。
nicegui==3.13.0
python-docx==1.1.2
pypdf==6.13.1
requests==2.34.2          # HIA 引擎调 DeepSeek 用(NiceGUI 不像 streamlit 那样自带,需显式列出)

# 旧 Streamlit 版(可选,部署 NiceGUI 不需要):
# streamlit==1.40.1
